- defaults:
    link:
      relink: true

# remove deadlinks in directory
- clean: ['~']

- link:
    ~/.tmux.conf: tmux.conf
    ~/.vim: vim
    ~/.vimrc: vimrc
    ~/.bashrc: bashrc
    ~/.zshrc: zshrc
    ~/.p10k.zsh: p10k.zsh

#- create:
#    - ~/downloads
#    - ~/.vim/undo-history

#- shell:
#    - [git submodule update --init --recursive, Installing submodules]
